{{ ansible_managed | comment }}

send host-name = "{{ inventory_hostname }}";

option rfc3442-classless-static-routes code 121 = array of unsigned integer 8;

request subnet-mask, broadcast-address, time-offset, routers,
	{% if domain_name is undefined or not domain_name %}domain-name,{% endif %}
	domain-search, domain-name-servers,
	dhcp6.name-servers, dhcp6.domain-search, dhcp6.fqdn, dhcp6.sntp-servers,
	netbios-name-servers, netbios-scope, interface-mtu,
	rfc3442-classless-static-routes, ntp-servers;

supersede host-name = "{{ inventory_hostname }}";

{% if domain_name is defined %}
supersede domain-name = "{{ domain_name }}";
prepend domain-search "{{ domain_name }}";
{% endif %}
